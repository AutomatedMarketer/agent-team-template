{
  "name": "agent-team-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The canonical AI agent team repo. Cloned by students, scaffolded by /onboard.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "audit:prompts": "node scripts/prompt-audit.mjs",
    "validate:runs": "node scripts/validate-run-log.mjs",
    "build:card": "node scripts/build-model-card.mjs",
    "sync:blocks": "node scripts/sync-prompt-blocks.mjs",
    "check:ledger": "node scripts/check-ledger.mjs",
    "check:proposals": "node scripts/check-proposals.mjs",
    "check:arming": "node scripts/check-arming.mjs"
  }
}
