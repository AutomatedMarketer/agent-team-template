# Nothing in this repo requires a local secret.
# Connectors (Gmail, Calendar, CRM) are authorised in claude.ai, not here.
# These names exist only for the optional local tooling a student may add later.

# Optional: only if the student runs the cockpit locally against their own repo.
GITHUB_REPO=
GITHUB_OWNER=
