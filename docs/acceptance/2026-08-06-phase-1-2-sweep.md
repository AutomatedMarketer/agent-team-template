# Phase 1 and 2 acceptance sweep

**Run:** 2026-08-06
**Repo:** `agent-team-template`, first build
**Method:** `npm test` for everything a machine can judge. Rows marked `pending` need a
live claude.ai account or a person reading output, and are honestly marked rather than
assumed.

---

## Phase 1 — the repo (criteria section 2)

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| R1 | Clone plus Claude Code produces a working orchestrator | pending | Needs a clean-machine clone and a live session. `CLAUDE.md` names all five specialists — `tests/orchestrator.test.mjs`. |
| R2 | Every path a routine needs is committed | pass | `tests/structure.test.mjs`. Nothing references `~/.claude/`; `skills/README.md` documents why skills live under `.claude/`. |
| R3 | `.claude/settings.json` sets model and effort | pass | `tests/settings.test.mjs` — `model: opus`, `effortLevel: high`, alias enforced. Startup-header confirmation is item 1 of the pending measurement. |
| R4 | `runs/` has a documented, parseable format | pass | `runs/README.md` plus `scripts/validate-run-log.mjs`; `tests/run-log.test.mjs` covers eight cases. |
| R5 | Each run log deep-links to its session | pass | Schema rejects a scheduled run with no `session_url`. URL shape confirmed in the pending measurement. |
| R6 | `shared/` holds business context all agents read | pass | `tests/agents.test.mjs` asserts every agent reads `shared/business-brain.md`. |
| R7 | No secret committed, ever | pass | `tests/secrets.test.mjs` scans the working tree and the full git history. |
| R8 | `.gitignore` covers `.env` before the first commit | pass | `.gitignore` written before `git add`; history scan in `tests/secrets.test.mjs`. |

## Phase 2 — every agent (criteria section 3.1)

| # | Criterion | Verdict | Evidence |
|---|---|---|---|
| A1 | One-line job description a non-technical person understands | pass | `tests/agents.test.mjs` — length, single line, and a jargon ban. |
| A2 | States its own model, as an alias | pass | Frontmatter checked against `AGENT_SPECS`; pinned ids rejected. |
| A3 | Carries the unattended-run block | pass | Byte-equality against `shared/standards/prompt-blocks/unattended-run.md`. |
| A4 | Carries the progress-grounding block | pass | Byte-equality, same test. |
| A5 | Never sends, posts, deletes, or spends without approval | pending | Stated and tested in text; the live attempt against each agent still has to be run. See `docs/safety/draft-only.md`. |
| A6 | Writes output and a run log in one commit | pass | Instructed in every agent file and in the run-log skill; `tests/agents.test.mjs` checks both references. |
| A7 | Passes the prompt audit | pass | `node scripts/prompt-audit.mjs` — clean across `CLAUDE.md`, all agents, all skills, all rules. |
| A8 | Produces something useful on an empty business context | pass | Every agent file carries an explicit empty-brain clause, checked by `tests/agents.test.mjs`. Live confirmation is part of the pending sweep. |

## Phase 2 — per agent (criteria section 3.2)

| Agent | Passes when | Verdict | Evidence |
|---|---|---|---|
| orchestrator | Routes three requests to three specialists, does the rest itself | pending | Routing table and three-step tree in place and tested; the live routing check needs a session. |
| research | Sourced report with live URLs, no setup | pending | No connector required by design, so this is the first one to confirm. |
| content | Recognisable as the student's voice | pending | Needs filled voice samples and a person who knows the student. |
| email | Every reply in drafts, zero sends | pending | Needs the Gmail connector. |
| customer-service | Answers from the FAQ, says "I don't know" otherwise | pending | Needs a filled FAQ. |
| sales | Works with no CRM connected | pass | `tests/sales.test.mjs` — the agent names `pipeline.md` and states the no-CRM default; the pipeline table ships with the repo. |

---

## What is left, and who can do it

| Item | Needs |
|---|---|
| Every `pending` row above | A live claude.ai account and one filled business brain |
| `docs/measurements/2026-08-06-routine-runtime.md` | The same account. This is the highest-value one. |
| `docs/safety/draft-only.md` deny list | The Gmail connector attached |

Nothing above blocks Phase 3 or Phase 4. The repo contract is frozen as of this sweep.
